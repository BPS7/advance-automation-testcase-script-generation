import { test, expect } from "@playwright/test";

test.describe("{{title}}", () => {
  {{#each cases}}
  test("{{description}}", async ({ page }) => {
    await page.goto({{{js ../route}}});
    {{#each steps}}
    {{{this}}}
    {{/each}}
    // Expectation: {{expected}}
  });
  {{/each}}
});

